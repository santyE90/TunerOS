# Shared contracts

This directory will hold deliberately language-neutral contracts or fixtures when more than one
TunerOS component needs them. It remains empty in Phase 2A because vehicle simulation, synthetic DME
publication, and in-memory CAN transport are C++-only and no cross-language decoding contract exists.
