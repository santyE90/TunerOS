# Shared contracts

This directory will hold deliberately language-neutral contracts or fixtures when more than one
TunerOS component needs them. It remains empty in Phase 1B because vehicle-side contracts and
simulation are authoritative in C++ and no cross-language application protocol has been defined.
